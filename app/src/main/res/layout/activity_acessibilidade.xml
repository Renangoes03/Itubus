<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".activity.activity.AcessibilidadeActivity">

    <include
        layout="@layout/toolbar"></include>

    <!-- Caixa de fundo para a primeira mensagem -->
    <TextView
        android:id="@+id/messageText1"
        android:layout_width="331dp"
        android:layout_height="80dp"
        android:layout_centerInParent="true"
        android:background="@drawable/message_box_background"
        android:gravity="center"
        android:padding="20dp"
        android:text="Este recurso será adicionado em uma atualização futura."
        android:textColor="@android:color/black"
        android:textSize="18sp"
        app:layout_constraintBottom_toTopOf="@id/messageText2"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Caixa de fundo para a segunda mensagem -->
    <TextView
        android:id="@+id/messageText2"
        android:layout_width="331dp"
        android:layout_height="80dp"
        android:layout_centerInParent="true"
        android:background="@drawable/message_box_background"
        android:gravity="center"
        android:padding="20dp"
        android:text="Já estamos trabalhando nisso....."
        android:textColor="@android:color/black"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/messageText1" />

</androidx.constraintlayout.widget.ConstraintLayout>
